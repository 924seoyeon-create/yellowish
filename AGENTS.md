# 노릇노릇

Static PWA — vanilla HTML/CSS/JS only, no framework, no build step. See `README.md` for structure
and how to run it locally, and `index.html` for the full product spec baked into the implementation.
